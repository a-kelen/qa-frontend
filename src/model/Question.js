export default class {
    constructor (id, title, description, is_pass, is_ignored) {
      this.id = id;
      this.email = email;
      this.description = description;
      this.is_ignored = is_ignored;
      this.is_pass = is_pass;
    }
  }
  